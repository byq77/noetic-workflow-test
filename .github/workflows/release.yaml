name: Bump version and dispatch a release
on: 
    workflow_dispatch:
jobs:
    hello-world:
        runs-on: ubuntu-latest
        steps:
            - name: Hello world action step
              run: echo "Hello world"      
              